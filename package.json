{
  "name": "xmosaic-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "desktop:dev": "electrobun dev",
    "desktop:run": "electrobun run",
    "desktop:build": "electrobun build",
    "desktop:build:stable": "electrobun build --env=stable",
    "desktop:typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "@types/three": "^0.181.0",
    "typescript": "^5.9.3"
  }
}
